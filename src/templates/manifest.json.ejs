{
  "id": "<%= plugin.id %>",
  "name": "<%= plugin.name %>",
  "description": "<%= plugin.description %>",
  "author": "<%= plugin.author %>",
  <% if (plugin.authorUrl) { %>"authorUrl": "<%= plugin.authorUrl %>",<% } %>
  <% if (plugin.fundingUrl) { %>"fundingUrl": "<%= plugin.fundingUrl %>",<% } %>
  "isDesktopOnly": <%= plugin.isDesktopOnly%>,
  "minAppVersion": "0.16.0",
  "version": "0.0.1"
}
